`Kernel32.dll` `HeapReAlloc` maps directly to `RtlReAllocateHeap`. All parameters are descripted in *Ms SDK*.

# Documented by

* Tomasz Nowak

# See also

* `RtlAllocateHeap`
* `RtlCreateHeap`
* `RtlFreeHeap`
