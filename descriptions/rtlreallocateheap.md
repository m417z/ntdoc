`Kernel32.dll` `HeapReAlloc` maps directly to `RtlReAllocateHeap`. All parameters are described in *Ms SDK*.

# Documented by

* Tomasz Nowak

# See also

* `RtlAllocateHeap`
* `RtlCreateHeap`
* `RtlFreeHeap`
